server {

    listen 80;

    server_name {PROJECT_NAME}.{PROJECT_DOMAIN};

    error_page 404 /404.html;
    location = /404.html {
        internal;
    }

    include /etc/nginx/vhost.d/modules/*.conf;

    error_log /var/log/nginx/error.log warn;
}
